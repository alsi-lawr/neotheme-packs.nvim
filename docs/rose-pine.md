# Rosé Pine mapping

Authority: `rose-pine/neovim` at `ff483051a47e27d84bdef47703538df1ed9f4a47`.
The Main, Moon, and Dawn records use Full mode because pinned defaults directly distinguish
diagnostics, Git state, headings, links, panels, and borders. Neotheme roles without a direct
default-group equivalent use the nearest palette or configured group token. Those assignments are
best-fit aliases, not exact upstream semantics. Every hexadecimal value is an exact pinned token.
